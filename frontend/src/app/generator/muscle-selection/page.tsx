import { MuscleSelectorContainer } from '@/components/muscle-selector/MuscleSelectorContainer';

export default function MuscleSelectionPage() {
    return (
        <main className="min-h-screen bg-transparent">
            {/* Navigation / Header Placeholder if needed, but Container handles layout */}
            <MuscleSelectorContainer />
        </main>
    );
}
